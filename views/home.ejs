<%- include('partials/header'); -%>

    <h1>Blog - Home</h1>

    <p> <%= homeContent %> </p> <br><br>

    <h2>Posts</h2>
    <% postsArr.forEach(function(post) { %>
        <h5><%= post.title %></h5>
        <p><%= post.post.substring(0, 100) + "..." %><a href="/posts/:<%=post.title%>">Read More</a></p>
        
    <% }); %>
    
<%- include('partials/footer'); -%>